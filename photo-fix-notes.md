# 人员肖像加载修复记录

原有部分肖像的存储文件名含有空格或非 ASCII 字符。预览环境会将这些路径重定向至受限对象，因此浏览器无法取得图像。已从用户补充的 Overleaf 项目提取肖像，为每个文件创建 ASCII 文件名副本并重新上传，再将页面引用替换为新的静态资源路径。

已逐一验证 15 个页面使用的肖像资源，均返回有效的 `image/jpeg` 或 `image/webp` 响应。桌面端与移动端全页截图确认：4 位确认受邀嘉宾、8 位组织委员会成员和 3 位顾问的照片均已显示。
